# repo-map — real CLI dogfood Project Spec (Section 52.1 / Appendix B / REQ-245).
# profiles = [] only. Destination basename MUST equal name.
# Generated trees stay outside this repository (independent dogfood repo).
schema = 1
name = "repo-map"
module = "github.com/robertguss/repo-map"
description = "Repository inventory CLI with text and JSON output"
archetype = "cli"
destination = "./repo-map"
profiles = []
