# Intentional failure for negative e2e / agent tests.
# Expected error id: spec.unknown_field
# Cause: top-level key `author` is not in Section 14.3 field contract.
schema = 1
name = "invalid-unknown-field"
module = "github.com/example/invalid-unknown-field"
description = "Spec with an unknown top-level field (must fail strict decode)"
archetype = "cli"
destination = "./invalid-unknown-field"
profiles = []
author = "not-a-foundry-field"
