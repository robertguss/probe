# Appendix B — Public CLI with distribution (profile existence is resolve-layer).
# SPEC-FOUNDRY-002 canonical example. Spec-layer validate accepts this shape.
schema = 1
name = "repo-map"
module = "github.com/robertguss/repo-map"
description = "Repository inventory CLI with text and JSON output"
archetype = "cli"
destination = "./repo-map"
visibility = "public"
profiles = ["distribution"]
