# P2.8 exit review — performance baselines citation

- **Exit bead:** `go-foundry-cli-5pr` (P2.8 — Phase 2 exit review)
- **Perf bead:** `go-foundry-cli-j8h.1` (P2.7.b — Performance baseline capture scripts + evidence logs)
- **Spec:** Section 49 / REQ-165

## Exit checklist item

P2.8 item 6: *Performance baselines recorded (j8h.1) without inventing hard gates.*

## Evidence pointers

| Artifact | Path |
| -------- | ---- |
| Machine-readable latest capture | [`baselines-latest.json`](baselines-latest.json) |
| Human summary | [`summary.md`](summary.md) |
| Format schema (stable for later thresholding) | [`schema.json`](schema.json) |
| Capture harness | [`scripts/perf/capture-baselines.sh`](../../../scripts/perf/capture-baselines.sh) |
| Optional CI (not required gate) | [`.github/workflows/perf-baselines.yml`](../../../.github/workflows/perf-baselines.yml) |
| Format lock test | `go test ./internal/archtest -run TestPerfBaselineSchema` |

## Policy

- **Absolute pass/fail thresholds: none.** `gates.absolute_thresholds` is JSON `null`.
- Warm + cold generate samples and write-free p50/p95 are recorded for later thresholding only after multi-machine history exists.
- P2.8 must **cite** this pack; it must **not** introduce millisecond gates from a single capture.

## How P2.8 should reference this

When writing `docs/evidence/P2-exit-review.md` (or equivalent), include:

```markdown
| 6 | Performance baselines recorded (j8h.1); no hard gates | PASS | [docs/evidence/perf/](perf/) — see [P2.8-citation.md](perf/P2.8-citation.md) |
```
