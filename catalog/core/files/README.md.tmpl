# [[.Name]]

[[.Description]]

## Build and run

```bash
go build -o bin/[[.Binary]] ./cmd/[[.Binary]]
./bin/[[.Binary]] --help
```

## Develop

Canonical commands (also in `AGENTS.md` and `docs/commands.md`):

```bash
gofmt -w .
go test -count=1 ./...
go vet ./...
go tool staticcheck ./...
```

## Docs

| Document | Role |
| -------- | ---- |
| [`AGENTS.md`](AGENTS.md) | Single agent instruction authority |
| [`docs/architecture.md`](docs/architecture.md) | Package layout and growth rules |
| [`docs/commands.md`](docs/commands.md) | Command reference |
| [`docs/testing.md`](docs/testing.md) | Testing conventions |
[[ if eq .Archetype "tui" -]]
| [`docs/ui-architecture.md`](docs/ui-architecture.md) | TUI lifecycle, effects, debug-log |
[[ end -]]

## CI

- **PR/default:** one Linux job (format, mod verify, tests, vet, Staticcheck)
- **Weekly/manual strict:** govulncheck, race (when available), macOS sample, fuzz when present
[[ if .DistributionEnabled ]]

## Releases

Public release scaffolding is enabled (`distribution` profile). See
[`docs/releasing.md`](docs/releasing.md). **Publication is blocked** until you
add and review a `LICENSE` and complete repository release settings (FND-019).
Foundry does not generate or validate licenses.
[[ end ]]

Generated by Foundry. Edit freely after generation (DEC-002).
