// Package version reports build identity for the generated project (Section 16.2).
package version

// Version is the semantic version string. Release builds may stamp this via
// Go build settings or -ldflags; unstamped local builds report this default.
var Version = "0.0.0-dev"

// Commit is the VCS revision when stamped; empty for unstamped local builds.
var Commit = ""
