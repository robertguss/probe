# Expected error id: spec.unknown_field
# Cause: unknown top-level field `author` (strict decoding).
schema = 1
name = "unknown-field"
module = "github.com/example/unknown-field"
description = "Unknown field fixture"
archetype = "cli"
destination = "./unknown-field"
author = "not-allowed"
profiles = []
