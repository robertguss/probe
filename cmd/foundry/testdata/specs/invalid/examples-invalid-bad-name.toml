# Intentional failure for negative e2e / agent tests.
# Expected error id: spec.invalid_field
# Cause: `name` must be lowercase ASCII kebab-case, 1–63 bytes, start with a
# letter, end alphanumeric (Section 15.1). Underscores are not allowed.
schema = 1
name = "bad_name"
module = "github.com/example/bad_name"
description = "Spec with an invalid project name (underscore not kebab-case)"
archetype = "cli"
destination = "./bad_name"
profiles = []
